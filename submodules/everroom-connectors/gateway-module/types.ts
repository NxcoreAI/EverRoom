import type { ConnectorProvider, NormalizedCalendarChange, NormalizedDocument, NormalizedMailChange, SyncMode } from "@nxcore/connector-contract";
export type { ConnectorProvider, NormalizedCalendarChange, NormalizedDocument, NormalizedMailChange, SyncMode } from "@nxcore/connector-contract";
export interface ConnectorConfig { nangoUrl:string; nangoSecret:string; gmailConfigKey:string; outlookConfigKey:string; googleDocsConfigKey:string; notionConfigKey:string; googleCalendarConfigKey:string; googleClientId:string; googleClientSecret:string; notionClientId:string; notionClientSecret:string; outlookClientId:string; outlookClientSecret:string; pollingIntervalMs:number; enabled:boolean; databasePath:string; /** 阶段二：注册表驱动的 provider → configKey（新增 provider 自动进入，无需新字段）。 */ providerConfigKeys:Record<string,string>; }
export interface PullPage { changes: NormalizedMailChange[]; calendarChanges?: NormalizedCalendarChange[]; documents?: NormalizedDocument[]; continuation?: string; terminalCursor?: string; }
export interface ConnectorExecutor { discoverScopes?(connection:{provider:ConnectorProvider;nangoConnectionId:string;nangoConfigKey:string}):Promise<Array<{id:string;displayName:string}>>; pull(scope: {provider:ConnectorProvider; nangoConnectionId:string; nangoConfigKey?:string; providerScopeId:string; sourceCursor:string|null}, mode:SyncMode): AsyncGenerator<PullPage>; }
