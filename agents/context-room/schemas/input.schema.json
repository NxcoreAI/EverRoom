{
  "type": "object",
  "properties": {
    "task": {
      "type": "string",
      "enum": ["room-enrich", "room-overview", "brief-refresh", "selection-rewrite", "merge-name"],
      "description": "The room task to execute."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120,
      "description": "Room title, required for room-enrich."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000,
      "description": "Room description, required for room-enrich."
    },
    "roomId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Target room id, required for room-overview and brief-refresh."
    },
    "roomTitle": {
      "type": "string",
      "maxLength": 120,
      "description": "Optional room title for context framing."
    },
    "documentName": {
      "type": "string",
      "maxLength": 200,
      "description": "Name of the document being edited, for selection-rewrite provenance."
    },
    "currentBrief": {
      "type": "object",
      "description": "Current room brief for brief-refresh; user-edited fields are authoritative."
    },
    "roomA": {
      "type": "object",
      "description": "First source room for merge-name: title, optional kind, background."
    },
    "roomB": {
      "type": "object",
      "description": "Second source room for merge-name: title, optional kind, background."
    },
    "selectedText": {
      "type": "string",
      "minLength": 1,
      "maxLength": 20000,
      "description": "The selected fragment to rewrite, required for selection-rewrite."
    },
    "contextBefore": {
      "type": "string",
      "maxLength": 4000,
      "description": "Document text before the selection, style context only."
    },
    "contextAfter": {
      "type": "string",
      "maxLength": 4000,
      "description": "Document text after the selection, style context only."
    },
    "instruction": {
      "type": "string",
      "maxLength": 2000,
      "description": "Rewrite instruction for selection-rewrite."
    },
    "blockType": {
      "type": "string",
      "maxLength": 64,
      "description": "Markdown block type of the selection, e.g. heading, bulletList, codeBlock."
    },
    "formatContext": {
      "type": "object",
      "description": "Editor format context of the selection (marks, list nesting, code language)."
    },
    "responseLanguage": {
      "type": "string",
      "maxLength": 35,
      "description": "BCP-47 language tag for natural-language output."
    },
    "writingStyle": {
      "type": "string",
      "maxLength": 2000,
      "description": "Trusted writing-style guidance section injected by the gateway for selection-rewrite; the user instruction always takes priority."
    },
    "outputContract": {
      "type": "object",
      "description": "Structured output examples supplied by the trusted room-overview workflow."
    },
    "rules": {
      "type": "array",
      "maxItems": 20,
      "items": {
        "type": "string",
        "maxLength": 1000
      },
      "description": "Additional trusted generation constraints supplied by the internal workflow."
    }
  },
  "required": ["task"],
  "additionalProperties": false
}
