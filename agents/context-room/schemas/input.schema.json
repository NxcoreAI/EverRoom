{
  "type": "object",
  "properties": {
    "task": {
      "type": "string",
      "enum": [
        "room-enrich",
        "room-overview",
        "brief-refresh",
        "merge-name"
      ],
      "description": "The room task to execute."
    },
    "title": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120,
      "description": "Room title, required for room-enrich."
    },
    "description": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2000,
      "description": "Room description, required for room-enrich."
    },
    "roomId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "Target room id. Required for room-overview and brief-refresh; also passed for room-enrich (memory_search may use it as room_id)."
    },
    "roomTitle": {
      "type": "string",
      "maxLength": 120,
      "description": "Optional room title for context framing."
    },
    "currentBrief": {
      "type": "object",
      "description": "Current room brief for brief-refresh; user-edited fields are authoritative."
    },
    "roomA": {
      "type": "object",
      "description": "First source room for merge-name: title, optional kind, background."
    },
    "roomB": {
      "type": "object",
      "description": "Second source room for merge-name: title, optional kind, background."
    },
    "responseLanguage": {
      "type": "string",
      "maxLength": 35,
      "description": "BCP-47 language tag for natural-language output."
    },
    "outputContract": {
      "type": "object",
      "description": "Structured output examples supplied by the trusted room-overview workflow."
    },
    "rules": {
      "type": "array",
      "maxItems": 20,
      "items": {
        "type": "string",
        "maxLength": 1000
      },
      "description": "Additional trusted generation constraints supplied by the internal workflow."
    }
  },
  "required": [
    "task"
  ],
  "additionalProperties": false
}
